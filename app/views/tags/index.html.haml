
.title
  %h1= t('tags.list')

.feature-tags
  - @feature_tags.each do |tag|
    .item
      .wrapper
        = link_to tag.name, tag_path(tag.name_seo)

.list-tags
  - @tags.each do |tag|
    .item
      = link_to tag.name, tag_path(tag.name_seo)
