
.title
  %h1= t('tags.list')

.feature-tags
  - @feature_tags.each do |tag|
    .item
      .wrapper
        = link_to tag.name, tag_path(tag.name_seo)

.tags
  - @tags.each do |tag|
    .item
      = link_to tag.name, tag_path(tag.name_seo)

= paginate @tags, :param_name => params[:tags_page]
