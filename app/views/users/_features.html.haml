
.feature-users
  %h2= t('users.list')
  .items
    - feature_users.each do |user|
      .item
        .wrapper
          - if user.profile_photo.blank?
            = link_to image_tag('missing.png'), user_path(user.username)
          - else
            = link_to image_tag(user.profile_photo.photo.url(:thumb)), user_path(user.username)
          %h3= link_to user.username, user_path(user.username)
