

.sidebar

  = render 'search'

  %ul.tags-menu.hide
    - @tags.each do |tag|
      %li= tag.name



.main-content
  .items
    - @reports.each do |r|
      .item
        %h2= link_to r.name, report_path(r)
        .subhead= r.subhead
        .hide.tag= r.tag.name
    

  = paginate @reports