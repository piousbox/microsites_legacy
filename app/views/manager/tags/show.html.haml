

%h2
  Tag `#{ @tag.name }`

%h3 Reports
%ul
  - @reports.each do |r|
    %li
      = link_to '[~]', edit_admin_reportt_path(r)
      = r.name
      (#{ r.name_seo })

%h3 Galleries
%ul
  - @galleries.each do |r|
    %li
      = link_to '[~]', edit_admin_galleryy_path(r)
      = r.name
      (#{ r.galleryname })