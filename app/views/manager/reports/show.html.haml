

%h3
  = link_to '[~]', edit_manager_report_path(@report)
  = @report.name
.a
  = @report.name_seo
.a
  - if @report.photo
    = image_tag @report.photo.photo.url(:thumb)

.descr
  = raw @report.descr

  