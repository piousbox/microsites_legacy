
.set-layout
  %ul
    %li= link_to 'mobi', "#{request.url.split('?').first}?layout=mobile&locale=#{@locale}"
    %li= link_to 'app', "#{request.url.split('?').first}?layout=application&locale=#{@locale}"
    %li= link_to 'mini', "#{request.url.split('?').first}?layout=application_mini&locale=#{@locale}"
