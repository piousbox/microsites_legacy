



#application__header

  .logo

  %h1= link_to @site.name, root_path

  %ul.main-menu
    %li.homepage= link_to 'Home', root_path
    - @site.tags.each do |t|
      %li{ :class => t.name_seo }= link_to t.name, second_tag_path(t)
    %li.about= link_to 'About', about_path

  .subhead= @site.subhead

  

  .clear