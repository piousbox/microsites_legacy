

.main-footer
  = render 'set_locale'

  .shtick
    %span ^__^

  = render 'login_register'

  = render 'set_mobile'

  - proc do # hidden
    %ul.menu
      %li= link_to t('g.about'), about_path, :class => :about
      %li= link_to t('g.privacy'), privacy_path, :class => :privacy

  .clear