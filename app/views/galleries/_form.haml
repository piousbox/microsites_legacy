
- url ||= manager_galleries_path

= form_for @gallery, :url => url do |f|

  .field
    = f.label :name
    = f.text_field :name

  .field
    = f.label :is_public
    = f.check_box :is_public

  .field
    = f.label :is_done
    = f.check_box :is_done

  .field
    = f.label :is_feature
    = f.check_box :is_feature

  .actions
    = f.submit
