
.gallery-show
  .title
    %h1
      Gallery
      = @gallery.name.titleize

  .items
    - @gallery.photos.fresh.each do |ph|
      = render 'photos/present', :photo => ph, :gallery => @gallery

  = render 'share_this'
  .clear