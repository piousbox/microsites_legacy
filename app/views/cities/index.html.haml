
= render 'features', :feature_cities => @feature_cities

.all-cities
  .inner
    #cities_index_canvas
    %ul
      - @cities.each do |city|
        %li
          = link_to city.name, city_path(city.cityname)

