
.feature-cities
  - @feature_cities.each do |city|
    .item
      .wrapper
        = link_to image_tag(city.profile_photo.photo.url(:two_hundred)), city_path(city.cityname)
        = link_to city.name, city

.clear
.center.wrapper= render 'ads/leaderboard_gray-blue-dark'

.all-cities
  .wrapper
    #cities_index_canvas
    -# %h3 All Cities
    %ul
      - @cities.each do |city|
        %li
          = link_to city.name, city_path(city.cityname)

.clear
.center.wrapper= render 'ads/leaderboard_gray-blue-dark'
