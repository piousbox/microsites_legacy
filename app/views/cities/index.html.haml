



%ul.feature-cities
  - @feature_cities.each do |city|
    %li
      = link_to city.name, city
      = link_to image_tag(city.profile_photo.photo.url(:small)), city
  
%h2 Cities
%ul.cities
  - @cities.each do |city|
    %li= link_to city.name, city
  
