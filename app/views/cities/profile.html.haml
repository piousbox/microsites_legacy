

- content_for :left_menu do
  %ul
    %li= link_to 'Calendar', 'javascript: void(0);', :class => :calendar_link
    %li= link_to 'Map', 'javascript: void(0);', :class => :map_link
    -# %li= link_to 'Power Map', '#power_map'

- content_for :left do

  .map-container#cities_show_canvas
    .calendar-container
    .reports-show
    .galleries-show
    .videos-show

- content_for :right do
  .reports.hide
    .items
    = link_to 'Load More', '#', :class => :more
  .galleries.hide
    .items
    = link_to 'Load More', '#', :class => :more
  .venues
    Venues
  .places.hide
    Places
    .items
    -# = link_to 'Load More', '#', :class => :more
  .events.hide
    .items
    -# = link_to 'Load More', '#', :class => :more
  .people.hide
    .items
  .videos.hide
    .items
    -# = link_to 'Load More', '#', :class => :more




  


//        - [].each do |i|
//          .item
//            %h3= link_to i.name, 'javascript:void(0);', :class => "#{i.name_seo} show-report"
//            = image_tag 'logo.png'
//            %span 
//              - begin
//                By #{ i.user.username } 
//              - rescue
//              - unless i.created_at.blank?
//                on #{ i.created_at }
//            %ul.tags
//              %li tag1
//              %li tag2 
//              %li tag3
    