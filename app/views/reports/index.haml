
.title
  %h1
    = t('reports.list')
    - if !@city.blank?
      = t('g.in')
      = @city.name

.col-1
  = render 'list', :reports => @reports

.col-2
  = render 'ads/src/skyscraper'

  &nbsp;

.c
.reports-footer.center
  = render 'ads/src/leaderboard'
