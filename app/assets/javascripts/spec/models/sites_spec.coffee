
describe "Sites Model Spec", ->

  beforeEach ->
    a = 'a'

  afterEach ->
    a = 'a'
    
  describe "models", ->
    it "there should be site model", ->
      expect( false ).toEqual( true )

      
           
      